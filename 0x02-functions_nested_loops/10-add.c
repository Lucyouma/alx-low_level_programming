#include "main.h"
/**
 * add - Entry point that adds two integers and returns the result
 *
 * @a: number being added
 *
 * @b: number being added
 *
 * Return: Always 0 (success)
 *
 */
int add(int a, int b)
{
	return (a + b);
}
