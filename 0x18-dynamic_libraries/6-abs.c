#include "main.h"
/**
 * _abs - Entry point computes an integers absolute zero value
 *
 * @n: the int to check
 *
 * Return: the absolute value of int
 *
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
